# MCP servers in this repo

| Server | Path | What it is |
| --- | --- | --- |
| **dp-asc** | [`mcp/dp-asc`](./mcp/dp-asc) | Full App Store Connect API via 4 meta-tools over an OpenAPI-generated catalog (1000+ ops). Bring your own ASC API key. |

MCPs are **not** Claude Code marketplace plugins. Install with `claude mcp add` (see each server’s README). Do not commit `.p8` keys or Issuer/Key IDs into this repo.
